version: "3.9"
services:
  app:
    build: .
    image: fastapi-app:dev
    ports:
      - "8000:8000"
    volumes:
      - app_data:/data
    env_file:
      - .env
    restart: unless-stopped
volumes:
  app_data:
